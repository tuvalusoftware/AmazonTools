LLM_PROVIDER=gemini
GOOGLE_API_KEY=AIzaxxxxxxxxxxxx
GEMINI_MODEL=google_genai/gemini-3.5-flash
LLM_VERBOSE=false
BROWSER_HEADLESS=true

CRON_SCHEDULE="0 * * * *"
TIMEZONE="Asia/Ho_Chi_Minh"

TARGET_ASINS="B08N5WRWNW,B09G9FPPHY"
MAX_PAGES=5
REQUEST_DELAY=2.0

OUTPUT_DIR=data
OUTPUT_FORMAT=json

# Amazon credentials — used by the scraper to auto-login
AMAZON_EMAIL=your_email@example.com
AMAZON_PASSWORD=your_password

LOG_LEVEL=INFO
LOG_DIR=logs

# SQLite registry
DB_PATH=data/tracker.db

# SMTP — use double-underscore to set nested SmtpSettings fields
SMTP__HOST=smtp.gmail.com
SMTP__PORT=587
SMTP__USER=your_email@gmail.com
SMTP__PASSWORD=your_app_password
SMTP__FROM_ADDR=your_email@gmail.com

# Email digest
EMAIL_TO=recipient@example.com
EMAIL_DIGEST_CRON=0 8 * * *

# Web UI
WEB_PORT=8080
WEB_BASE_URL=http://localhost:8080
