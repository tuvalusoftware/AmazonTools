services:
  bsr-tracker:
    # Pull the image built by CI (.github/workflows/docker-build.yml) instead
    # of building locally.
    image: ghcr.io/tuvalusoftware/amazontools:main
    build: !reset null
    restart: always
